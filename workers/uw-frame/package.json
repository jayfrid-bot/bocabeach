{
  "name": "uw-frame",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud-side underwater sea-cam frame courier (Cloudflare Browser Rendering)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test-scheduled": "wrangler dev --test-scheduled"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260724.1",
    "wrangler": "^4.109.0"
  }
}
