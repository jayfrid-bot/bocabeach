{
  "name": "uw-frame",
  "version": "1.0.0",
  "private": true,
  "description": "Cloud-side multi-cam frame courier + Deerfield lifeguard-flag reader (Cloudflare Browser Rendering)",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test-scheduled": "wrangler dev --test-scheduled",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.0.2"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260724.1",
    "vitest": "^5.0.0",
    "wrangler": "^4.109.0"
  }
}
